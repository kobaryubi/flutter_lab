import 'package:flutter/widgets.dart';

class RiverpodScreen extends StatelessWidget {
  const RiverpodScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Riverpod Screen'));
  }
}
