import 'package:flutter/widgets.dart';

/// Tab 2 of the logged-in home shell. Placeholder used to verify
/// shell-route branch-swap behavior.
class LoggedInHomeTab2Screen extends StatelessWidget {
  const LoggedInHomeTab2Screen({super.key});

  @override
  Widget build(BuildContext context) => const Center(child: Text('Tab2'));
}
