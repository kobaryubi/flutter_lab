/// Represents where a URL should be opened.
enum NavigationDestination {
  /// Open the URL in an in-app WebView.
  webView,

  /// Open the URL in the device's external browser.
  externalBrowser,
}
