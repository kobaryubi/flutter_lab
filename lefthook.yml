pre-commit:
  exclude:
    - "package/*"
  jobs:
    - name: format
      glob: "*.dart"
      run: fvm dart format {staged_files}
      stage_fixed: true
    - name: fix
      run: fvm dart fix --apply
    # - name: analyze
    #   run: fvm flutter analyze
