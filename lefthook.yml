pre-commit:
  exclude:
    - "package/*"
  jobs:
    - name: format
      glob: "*.dart"
      run: fvm dart format {staged_files}
      stage_fixed: true
    - name: fix
      run: fvm dart fix --apply
      stage_fixed: true
