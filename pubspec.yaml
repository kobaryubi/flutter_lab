name: flutter_lab
description: Flutter lab project

publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.8.1
  flutter: '>=3.32.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
