name: flutter_lab
description: Flutter lab project

publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.3.4

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5

flutter:
  uses-material-design: true
