name: flutter_lab
description: Flutter lab project

publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.8.1
  flutter: '>=3.32.0 <4.0.0'

dependencies:
  english_words: ^4.0.0
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  freezed_annotation: ^3.1.0
  go_router: ^16.1.0
  http: ^1.5.0
  json_annotation: ^4.9.0
  provider: ^6.1.5

dev_dependencies:
  flutter_lints: ^6.0.0
  build_runner: ^2.6.0
  freezed: ^3.2.0
  json_serializable: ^6.10.0

flutter:
  assets:
    - assets/activities.json
    - assets/destinations.json
