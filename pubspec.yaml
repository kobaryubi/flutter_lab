name: flutter_lab
description: Flutter lab project

publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.4.1

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
